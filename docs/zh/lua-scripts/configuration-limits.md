---
translated_from: 827e532e2b0324591f0fdbb61a39e61180642b24
---

# 配置限制

- 位图可用 **2MB**（仅在 X20 上，一张全屏位图就约占用 768K）。
- Lua 脚本可用 **2MB** —— 实际使用中这是相当充裕的额度。

!!! tip "脚本中的位图"
    避免在 RAM 中保留大量位图数据。建议采用**延迟加载**方式 —— 仅在确实需要时才加载位图，随后将其缓存在内存中供下次使用，而不是反复从 SD card/eMMC 重新读取。
