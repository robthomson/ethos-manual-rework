# Basic Widget Layout

The minimal structure of a custom display widget script (`name`, `create`,
`paint`, `configure`, `read`/`write` for persisted settings) and how it's
placed on a screen alongside built-in widgets from
[Displays](../displays/index.md).

!!! note "Draft"
    This page is a placeholder — full content coming in a later pass.
