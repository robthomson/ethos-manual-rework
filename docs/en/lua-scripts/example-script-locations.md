# Example Script Locations

Where Ethos looks for scripts on the SD card (`/scripts`, per-model script
folders, widget vs. tool vs. system script locations) and what determines
where an installed script shows up in the UI.

!!! note "Draft"
    This page is a placeholder — full content coming in a later pass.
